cmake wants the path to ghc libraries (something like -D HASKELL_PATH='/path/to/.../lib/ghc-8.2.2/'). The folder is supposed to contain rts and ghc-prim for example.

To run, you need LD_LIBRARY_PATH=.